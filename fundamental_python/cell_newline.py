#!/usr/bin/env python

import sys
import csv

d = csv.reader(sys.stdin)
for l in d:
    maxlen = 0
    items = [item for item in l]
    for item in items
        item = item.split('\r')
        if len(item) > maxlen:
            maxlen = len(item)
    for item in items:
        
    print l
